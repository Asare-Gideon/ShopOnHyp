import React from 'react'
import { View, Text, TouchableOpacity } from 'react-native'
import { itemProp } from '../Types'

const Items = ({image,text}: itemProp) => {
    return (
        <TouchableOpacity>
         <Text>hello</Text>
        </TouchableOpacity>
    )
}

export default Items
