import { StyleSheet } from "react-native";
import { Colors, Sizes } from "../../constants/Layout";


export const stylse = StyleSheet.create({
    main: {
        paddingTop: 24,
        backgroundColor: Colors.warmWhite,
        width: Sizes.width,
        flex: 1,
        zIndex:666
    }
})